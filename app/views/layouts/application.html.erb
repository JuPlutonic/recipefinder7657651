<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title><%= yield(:title) %></title><!--  RecipeFinderForFork2forkcom -->
    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
      <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
        <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
      <%= csrf_meta_tags %>
      <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/navbar' %>
    <div class="container" style="margin-top: 60px">
      <div class="row">
        <div class="col-md-3 well">
          <p class="text-uppercase"><strong>Some Favorites</strong></p>
          <span><a class="badge badge-default" href="http://www.pairuptocode.com/exercises/bootstrap_before.html#">Celery Root</a></span>
          <span><a class="badge badge-default" href="http://www.pairuptocode.com/exercises/bootstrap_before.html#">Spaghetti Squash</a></span>
          <span><a class="badge badge-default" href="http://www.pairuptocode.com/exercises/bootstrap_before.html#">Killer Mushrooms</a></span>
          <%= form_tag('/', class: 'form-inline', method: 'get') do %>
            <div class="row">
              <div class="form-group col-md-8 col-md-offset-2">
                <%= text_field_tag :looking_for, @search_term, placeholder: "Enter veggie's name", class: 'input input-sm', style: 'width: 100%' %>
              </div>
            </div>
            <div class="row">
              <div class="form-group col-md-8 col-md-offset-2">
              <%= submit_tag 'Search recipes', name: nil, class: 'btn-danger btn-sm' %>
              </div>
            </div>
          <% end %>

        </div>
        <div class="col-md-9">
          <% content_for :examples do %>
          <div class="alert bg-danger alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <strong>Well!</strong> Have you had your daily dose of veggies today??
          </div>
          <% end %>
          <% if yield(:enabled_examples) == 'true' %>
          <%= yield(:examples) %>
          <% end %>
          <blockquote>
            <p>Take care of your body. It's the only place you've to live
            </p>
            <small>Jim Rohn</small>
          </blockquote>
        </div>

        <% if yield(:enabled_examples) == 'true' %>
        <%= render 'shared/examples' %>
        <% end %>

      </div>
    </div>
    <div class="container"><%= yield %></div>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  </body>
  <hr>
  <footer>
    <p class="text-center">Powered By Food2Fork.com</p>
  </footer>
</html>
